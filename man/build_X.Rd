% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_X.R
\name{build_X}
\alias{build_X}
\title{Build a genotype matrix from chromosome-specific PLINK files}
\usage{
build_X(
  SNPInfo,
  bfile_pattern,
  keep_file = NULL,
  plink = "./plink",
  temp_root = tempdir(),
  clumping = FALSE,
  r2 = 0.81,
  verbose = FALSE
)
}
\arguments{
\item{SNPInfo}{Data frame containing columns `CHR` and `SNP`.}

\item{bfile_pattern}{PLINK file prefix containing the placeholder
\code{\{CHR\}}.}

\item{keep_file}{Optional PLINK keep file.}

\item{plink}{Path to the PLINK executable.}

\item{temp_root}{Directory used for temporary PLINK files.}

\item{clumping}{Whether to remove highly correlated SNPs with PLINK
clumping. All SNPs receive the same p-value, and the clump radius is fixed
at 1000 kb, so this performs LD filtering only.}

\item{r2}{PLINK clumping r-squared threshold. Used only when `clumping` is
`TRUE`.}

\item{verbose}{Whether to display PLINK output.}
}
\value{
A list containing `X`, the SNP information retained by PLINK,
  `FAM`, `SNP_missing`, and `SNP_clumped`. `SNP_missing` contains requested
  SNPs absent from the source BED files, while `SNP_clumped` contains SNPs
  removed by LD clumping. Rows of `X` correspond to rows of `FAM`.
}
\description{
Extracts requested SNPs from chromosome-specific PLINK BED files, merges
the extracted files, fills missing genotypes with the A2 allele, and returns
a dense genotype matrix.
}
