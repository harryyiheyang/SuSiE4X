% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/estimate_prior_variance.R
\name{estimate_prior_variance}
\alias{estimate_prior_variance}
\title{Estimate the SuSiE prior variance from selected variants}
\usage{
estimate_prior_variance(
  X,
  y,
  Z,
  PRS,
  SNPInfo,
  family = NULL,
  status = NULL,
  mgcv_model = NULL,
  n_threads = 4,
  suff_block_size = 10000L,
  r_thres = 0,
  max_iter = 100L,
  tol = 1e-06
)
}
\arguments{
\item{X}{Standardized numeric genotype matrix with SNP IDs as column names.}

\item{y}{Outcome accepted by [SuSiE4I()]. A Cox outcome may be supplied as a
`survival::Surv` object.}

\item{Z}{Non-empty numeric covariate matrix that does not contain PRS
columns. `NULL` is not supported because the nuisance predictor must be
fixed before projection.}

\item{PRS}{Numeric matrix with 22 or 23 chromosome-specific PRS columns, or
`NULL`. Column names must exactly match `SNPInfo$CHR` values used by `X`.}

\item{SNPInfo}{Data frame containing `SNP` and `CHR`. Its SNPs must match
`colnames(X)` exactly.}

\item{family}{Outcome family accepted by [SuSiE4I()].}

\item{status}{Optional event indicator for Cox models when `y` is a time
vector.}

\item{mgcv_model}{`NULL`, `"gam"`, or `"bam"` for mgcv nuisance fits.}

\item{n_threads}{Number of threads used for matrix operations.}

\item{suff_block_size}{Row-block size for sufficient-statistic cross-products.}

\item{r_thres}{Absolute within-chromosome genotype-correlation threshold.
The default `0` uses the independent-variant estimator. Positive values
use chromosome-block sparse sufficient statistics.}

\item{max_iter}{Maximum number of EM updates for the prior variance.}

\item{tol}{Relative EM convergence tolerance.}
}
\value{
A list with `prior_variance`, `scaled_prior_variance`, and a one-row
  `diagnostics` data frame. The recommended package use is
  `susie_para_main = list(prior_variance = out$prior_variance,
  estimate_prior_variance = FALSE)`. The scaled value is retained for direct
  `susieR::susie_ss()` use with this estimator's sufficient-statistic
  convention. Do not rerun this estimator inside the outer iterations.
}
\description{
Fits the outcome once using `Z` and the all-chromosome PRS, then constructs
chromosome-specific LOCO sufficient statistics for the columns of `X`.
The columns of `X` must already be standardized.
}
